<form method="post" action="{{ path('app_etiquette_delete', {'id': etiquette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etiquette.id) }}">
    <button class="btn">Delete</button>
</form>
