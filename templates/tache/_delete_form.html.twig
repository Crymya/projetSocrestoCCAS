<form method="post" action="{{ path('app_tache_delete', {'id': tache_realise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tache_realise.id) }}">
    <button class="btn">Delete</button>
</form>
