{% extends 'base.html.twig' %}

{% block title %}TacheRealise index{% endblock %}

{% block body %}
    <h1>TacheRealise index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Moment</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for tache_realise in tache_realises %}
            <tr>
                <td>{{ tache_realise.id }}</td>
                <td>{{ tache_realise.moment ? tache_realise.moment|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_tache_show', {'id': tache_realise.id}) }}">show</a>
                    <a href="{{ path('app_tache_edit', {'id': tache_realise.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tache_new') }}">Create new</a>
{% endblock %}
